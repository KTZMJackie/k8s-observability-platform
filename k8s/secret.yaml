apiVersion: v1
kind: Secret
metadata:
  name: fastapi-secret
type: Opaque
stringData:
  api-key: ""  # Populate at deploy time — never commit real values
  # kubectl create secret generic fastapi-secret --from-literal=api-key=$API_KEY
